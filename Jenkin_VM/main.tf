module "VPC-Jenkin" {
    source = "../Template"

    #Instance Variable - using default for now, uncommet to change the variable

    #region
    #instance_type
    #key_name
    #VM_Count

    #VPC Variable

    #cidr
    #instance_tenancy
    #enable_dns_hostnames
    #enable_dns_support
    #enable_classiclink
    #tags
}